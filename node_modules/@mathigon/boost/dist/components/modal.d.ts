import { CustomElementView } from '../';
/**
 * Modal component
 */
export declare class Modal extends CustomElementView {
    private isOpen;
    private $iframe?;
    private $video?;
    canClose: boolean;
    ready(): void;
    open(noAnimation?: boolean): void;
    close(keepBg?: boolean, noEvent?: boolean): void;
    getOpenModal(): Modal | undefined;
}
