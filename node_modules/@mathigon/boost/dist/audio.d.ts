export declare class AudioPlayer {
    src: string;
    defaultVolume: number;
    private player;
    constructor(src: string, defaultVolume?: number, preload?: boolean);
    play(volume?: number): void;
}
